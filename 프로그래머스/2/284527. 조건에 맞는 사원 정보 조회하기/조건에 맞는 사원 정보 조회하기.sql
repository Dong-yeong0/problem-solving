SELECT
	SUM(SCORE) AS SCORE,
    EMP_NO,
    EMP_NAME,
    POSITION,
    EMAIL
FROM
	HR_EMPLOYEES
JOIN
	HR_DEPARTMENT
USING
	(DEPT_ID)
JOIN
	HR_GRADE
USING
	(EMP_NO)
WHERE
	YEAR = 2022
GROUP BY
	EMP_NO,
    EMP_NAME,
    POSITION,
    EMAIL
ORDER BY
	SUM(SCORE) DESC
LIMIT
	1